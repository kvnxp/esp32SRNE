{
    "tasks": [
        {
            "label": "closeSerial",
            "command":"${command:arduino.closeSerialMonitor}",
        },
        {
            "label": "openSerial",
            "command": "${command:arduino.openSerialMonitor}"
        }
    ],
    "version": "2.0.0"
}